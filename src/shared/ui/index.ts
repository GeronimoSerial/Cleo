// Shared UI - Barrel Export
// Primitive UI components (shadcn/ui) and magic UI components
// These are reusable across all features

// Re-export all UI primitives
export * from './ui';

// Re-export magic UI components
export * from './magicui';
